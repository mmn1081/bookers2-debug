<%= form_with model: [book, book_comment], local: false do |f| %><!--jsにするためにlocal: falseと記述　HTMLはlocal: trueと記述-->
  <%= f.text_area :comment, rows: '5',placeholder: "コメントをここに", class: "w-100" %>
  <%= f.submit "送信" %>
 <% end %>